<section class="empty-state reveal">
  <div class="empty-state__card">
    <p class="empty-state__eyebrow"><%= eyebrow || 'Sin resultados' %></p>
    <h3><%= title || 'No encontramos piezas que coincidan con tu busqueda.' %></h3>
    <p><%= message || 'Prueba con otra talla, rango de precio o termino de busqueda.' %></p>
    <a href="/catalogo">Limpiar filtros</a>
  </div>
</section>
